# Playwright E2E (placeholder)

Run locally:
- cd qa/e2e
- npm install
- npx playwright test --project=chromium

Environment variables expected:
- BASE_URL (default http://localhost:3000)
- AUTH_USER, AUTH_PASS (optional test creds)

This scaffold uses placeholder selectors and must be updated to match the real app.