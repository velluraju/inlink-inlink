# Inlink

Monorepo scaffold for Inlink. This workspace contains placeholders and local dev scaffolding.

Next steps:
- Fill in backend and frontend implementations under packages/
- Add repository on GitHub and push; then add secrets in GitHub Actions
- Run locally using docker-compose or devcontainer

Helpful commands:
- Start services: docker-compose up --build
- Backend dev: cd packages/backend && pnpm dev
- Frontend dev: cd packages/frontend && pnpm dev

Placeholders used for Paytm sandbox; replace with real sandbox keys in GitHub Secrets when ready.